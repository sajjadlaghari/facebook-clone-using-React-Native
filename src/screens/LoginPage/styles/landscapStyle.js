
import { StyleSheet } from 'react-native';

export default landscapStyle = (w, h) => {

    return StyleSheet.create({
        container:
        {
            flex: 1,
            backgroundColor: '#fff',
        },
      
     })
}